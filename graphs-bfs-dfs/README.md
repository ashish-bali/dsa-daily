# Graphs (BFS / DFS)

Traversal-based problems: connected components, shortest path on unweighted
graphs (BFS), cycle detection and topological order (DFS), grid flood fill.

_No problems yet. Add the first as `001-problem-slug/`._
