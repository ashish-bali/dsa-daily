# Sliding Window

Problems over contiguous subarrays or substrings where a window expands and
contracts to maintain an invariant (fixed size, distinct chars, sum ≤ k).

_No problems yet. Add the first as `001-problem-slug/`._
